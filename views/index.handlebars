<h1>{{title}}</h1>
<h4>Recipes</h4>
<ul class="list-group">
    {{#each recipies}}
    <li class="list-group-item">{{this.name}}: {{this.ingredients}} : {{this.instructions}}</li>
    {{/each}}
</ul>

